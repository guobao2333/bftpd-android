--- a/sysdeputil.c
+++ b/sysdeputil.c
@@ -1,15 +1,8 @@
 #include "sysdeputil.h"
 #include "sysutil.h"
 #include "str.h"
-#include <utmp.h>
-#include <utmpx.h>
-#ifdef VSF_BUILD_PAM
-#include <security/pam_appl.h>
-#include <security/pam_misc.h>
-#endif
-#ifdef VSF_BUILD_CAPABILITIES
-#include <sys/capability.h>
-#endif
+/* Android/Bionic: utmp, utmpx, PAM, libcap not available */
 
 int
 vsf_sysdep_check_auth(struct mystr* p_user_str,
                       struct mystr* p_pass_str,
                       struct mystr* p_remote_str)
 {
-#ifdef VSF_BUILD_PAM
-  int retval;
-  pam_handle_t* pamh = NULL;
-  struct pam_conv conv = { misc_conv, NULL };
-
-  retval = pam_start("bftpd", str_getbuf(p_user_str), &conv, &pamh);
-  if (retval == PAM_SUCCESS)
-    retval = pam_authenticate(pamh, 0);
-  if (retval == PAM_SUCCESS)
-    retval = pam_acct_mgmt(pamh, 0);
-  pam_end(pamh, retval);
-  return (retval == PAM_SUCCESS);
-#else
   return vsf_db_check_auth(p_user_str, p_pass_str, p_remote_str);
-#endif
 }
 
 void
 vsf_sysdep_log_login(const struct mystr* p_user_str,
                      const struct vsf_sysutil_sockaddr* p_addr)
 {
-  struct utmp ut;
-  struct utmpx utx;
-  memset(&ut,  0, sizeof(ut));
-  memset(&utx, 0, sizeof(utx));
-  strncpy(ut.ut_user,  str_getbuf(p_user_str), sizeof(ut.ut_user)  - 1);
-  strncpy(utx.ut_user, str_getbuf(p_user_str), sizeof(utx.ut_user) - 1);
-  ut.ut_type  = USER_PROCESS;
-  utx.ut_type = USER_PROCESS;
-  setutent();
-  pututline(&ut);
-  endutent();
+  /* no-op: utmp/utmpx not available on Android/Bionic */
+  (void)p_user_str;
+  (void)p_addr;
 }
 
 void
 vsf_sysdep_log_logout(const struct mystr* p_user_str)
 {
-  struct utmp ut;
-  memset(&ut, 0, sizeof(ut));
-  strncpy(ut.ut_user, str_getbuf(p_user_str), sizeof(ut.ut_user) - 1);
-  ut.ut_type = DEAD_PROCESS;
-  setutent();
-  pututline(&ut);
-  endutent();
+  /* no-op: utmp not available on Android/Bionic */
+  (void)p_user_str;
 }
 
-#ifdef VSF_BUILD_CAPABILITIES
 void
 vsf_sysdep_adopt_capabilities(unsigned int caps)
 {
-  cap_t cap = cap_init();
-  cap_value_t cap_list[2];
-  cap_list[0] = CAP_NET_BIND_SERVICE;
-  cap_list[1] = CAP_SETUID;
-  cap_set_flag(cap, CAP_EFFECTIVE, 2, cap_list, CAP_SET);
-  cap_set_proc(cap);
-  cap_free(cap);
+  /* no-op: libcap not used in Android app context */
+  (void)caps;
 }
-#endif
